<% if current_user.is_admin? %>
  <div class="container">
    <%= render 'matchdays/show_week' %>
  </div>
<% else %>
  <div class="container">
    <%= render 'matchdays/show_student' %>
  </div>
<% end  %>
